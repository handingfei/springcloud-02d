<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.handingfei.userprovide.mapper.UserMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.handingfei.userprovide.entity.User">
        <id column="uid" property="uid" />
        <result column="uname" property="uname" />
        <result column="passwd" property="passwd" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        uid, uname, passwd
    </sql>

    <resultMap id="base" type="com.handingfei.userprovide.entity.User">
        <id column="uid" property="uid" />
        <result column="uname" property="uname" />
        <result column="passwd" property="passwd" />
    </resultMap>

    <select id="list1" resultMap="base">
        select * from user
    </select>

</mapper>
